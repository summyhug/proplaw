# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Data validation
pydantic>=2.7.0

# LLM
anthropic>=0.26.0

# Knowledge graph (build, explore, visualize, audit — see propra/graph/README.md)
networkx>=3.3

# Vector search
faiss-cpu>=1.8.0

# Embeddings (for RAG — required to generate FAISS vectors)
sentence-transformers>=3.0.0

# PDF parsing (extract text from LBO/MBO source PDFs; propra/data/extract_pdf.py)
pdfplumber>=0.11.0

# Environment variables
python-dotenv>=1.0.0

# HTTP client (for testing and internal requests)
httpx>=0.27.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0

# Graph serialisation (save/load the KG without rebuilding from scratch)
# NetworkX supports pickle natively; joblib used for larger objects
joblib>=1.4.0

# Graph visualisation (propra/graph/visualize_html.py — interactive HTML export)
pyvis>=0.3.2
matplotlib>=3.9.0
