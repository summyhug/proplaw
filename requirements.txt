# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Data validation
pydantic>=2.7.0

# LLM
anthropic>=0.26.0

# Knowledge graph
networkx>=3.3

# Vector search
faiss-cpu>=1.8.0

# Embeddings (for RAG — required to generate FAISS vectors)
sentence-transformers>=3.0.0

# PDF parsing (for extracting text from LBO source documents)
pdfminer.six>=20221105

# Environment variables
python-dotenv>=1.0.0

# HTTP client (for testing and internal requests)
httpx>=0.27.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0

# Graph serialisation (save/load the KG without rebuilding from scratch)
# NetworkX supports pickle natively; joblib used for larger objects
joblib>=1.4.0

# Visualisation (optional — useful during development to inspect the graph)
matplotlib>=3.9.0
